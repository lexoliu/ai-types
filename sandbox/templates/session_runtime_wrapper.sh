mkdir -p {{ tmp_dir }} {{ cache_dir }} {{ bun_cache_dir }} {{ config_dir }} {{ playwright_cache_dir }} && export TMPDIR={{ tmp_dir }} TMP={{ tmp_dir }} TEMP={{ tmp_dir }} HOME={{ home_dir }} XDG_CACHE_HOME={{ cache_dir }} XDG_CONFIG_HOME={{ config_dir }} BUN_INSTALL_CACHE_DIR={{ bun_cache_dir }} PLAYWRIGHT_BROWSERS_PATH={{ playwright_cache_dir }} PYTHONPATH={{ python_path }}${PYTHONPATH:+:$PYTHONPATH} && {{ script }}
